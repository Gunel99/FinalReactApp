import { IDaysProps } from '../../shared/Days/types';
import { ITempProps } from '../../shared/Temperature/types';
import IconCodeProps from '../../utils/Icon/types';

export type WeatherBodyProps = IDaysProps & ITempProps & IconCodeProps;